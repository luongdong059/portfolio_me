import 'dart:ui';

class AppColors{
  // Main
  static const Color primaryColor = Color(0xff111827);
  // static const Color secondaryColor = Color(0xff111827);

  // Text
  static const Color textPrimary = primaryColor;
  static const Color textWhite = Color(0xffF9FAFB);

  // Buttons and Icons
  static const Color primaryButton = primaryColor;
  static const Color availableColor = Color(0xff0CB981);

  // Backgrounds and Cards
  static const Color backgroundColor = Color(0xffFFFFFF);
  static const Color componentColor = Color(0xffE5E7EA);
  static const Color tooltipColor = Color(0xffE5E7EA);
}